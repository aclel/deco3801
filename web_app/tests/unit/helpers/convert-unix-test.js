import {
  convertUnix
} from '../../../helpers/convert-unix';
import { module, test } from 'qunit';

module('ConvertUnixHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertUnix(42);
  assert.ok(result);
});
